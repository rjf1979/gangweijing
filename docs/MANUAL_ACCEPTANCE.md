# 人工确认清单（验收清单）

> 每个任务完成后，我会在这里记录需要你人工验收的确认项。
> 验收方式：按步骤操作，核对「预期结果」；全部符合即可通过。

---

---

## 2026-08-10 · 管理后台简历模板：AI 生成后台排队执行（关弹窗继续）+ 职位筛选 header【本地验证 PASS，待用户确认】

**环境**：管理后台 http://127.0.0.1:3216（本地）→ 生产 /admin/

### 开发要求（长期有效，禁止回退）
- 「简历模板」页顶部新增**职位筛选 header**：平铺「全部」+ 各职位 chips，点击职位只展示该职业模板分组，点「全部」恢复查看全部职业
- AI 生成新排版改为**后台任务异步执行**：点「开始生成」后接口立即返回，关闭面板/弹窗/离开页面都不影响，任务在服务端 FIFO 队列串行执行，完成后前端自动刷新列表
- **连续点击多次「AI 生成」会排队**：按提交顺序依次生成（同一时刻只跑一个），每套都会生成，不互相覆盖

### 确认项 1：职位筛选 header
- 操作：管理后台 → 简历模板
- 预期：顶部有「职位筛选」header，平铺「全部」+ 各职位 chips；默认「全部」高亮并展示全部职业模板分组；点某职位 chip 后左侧列表只剩该职业分组，chip 高亮；点「全部」恢复所有分组
- 本地验证（2026-08-10 PASS，headless Chrome CDP）：chips=全部+10 职位；逐个点击 10 个职位均只剩对应 1 组；点「全部」恢复 10 组

### 确认项 2：AI 生成后台执行（关闭弹窗继续生成）
- 操作：选中某职业模板 → 「AI 生成新排版」→ 填风格要求 → 点「开始生成」
- 预期：面板立即关闭，顶部出现「AI 生成后台执行中（N 个）：…，关闭面板/弹窗不影响，完成后自动刷新列表」；关闭面板/刷新/离开页面均不影响后台任务；完成后列表自动刷新并提示「AI 已生成「…」（未覆盖现有模板）」
- 本地验证（2026-08-10 PASS，CDP）：提交后面板关闭 + 后台执行提示出现；约 1-2 分钟后任务完成、提示出现，tech 分组模板数 4→5

### 确认项 3：连续点击排队
- 操作：连续点 2 次「开始生成」（同一职业或不同职业均可）
- 预期：每次提交立即返回，页面显示「后台执行中（2 个）」；任务按提交顺序依次执行（第一个 running 时第二个 pending），最终全部完成且每套都生成；任务列表/队列最终为空
- 本地验证（2026-08-10 PASS，API）：连续提交 2 个 tech 任务均立即 200 返回 jobId（queued=2，1 running + 1 pending）；约 3 分钟后两任务均 done 各生成 1 套（tech 模板 2→4 套）；GET /api/admin/resume-templates/generate-jobs 队列 queued=0

## 2026-08-10 · 前端退出登录 + 按套用模板字段结构编辑简历 + 模板套用/切换（PC 端）【本地验证 PASS，待用户确认】

**环境**：PC http://127.0.0.1:3215（本地）→ 生产 gwj.zhicha.io

### 开发要求（长期有效，禁止回退）
- 前端页头新增「退出登录」：调 POST /api/logout（服务端删除会话 + 清 cookie），本地清理草稿后跳转 /login，路由守卫拦截未认证访问
- 编辑简历改为**按套用模板的字段结构编辑**：套用哪套模板，就按该模板用到的字段结构编辑（基础信息 12 个独立字段 + 各区块字段），数据对号入座回填表单并实时渲染到该模板；不再按上传原文自由拆分、也不再按职业固定分区编辑
- **用户基础信息拆解为独立字段保存**：姓名/性别/出生年月/电话/邮箱/所在地/在职公司/职位/工作年限/期望薪资/求职意向/到岗时间，保存时写入 app_users.resume_basic（jsonb），独立于结构化 JSON，编辑页与模板渲染按字段读取
- 前端「我的简历」「编辑简历」可选择并切换管理后台已生成的简历模板：默认套用该职业默认模板，模板列表（同职业全部模板）内可自由切换；**切换持久化到用户**（app_users.preferred_template_id），再次进入「我的简历」/「编辑简历」默认加载上次套用的模板；后台 is_default 保持不变
- 「我的简历」页统一提供**简历头像上传**：支持 JPG/PNG/WebP/GIF、≤5MB，可上传/更换（前端不提供删除按钮）；头像独立落库（app_users.avatar_path/avatar_mime/avatar_updated_at + 磁盘 avatar-files/），GET /api/resume 返回 avatar.url
- 头像按**所套用模板**对号入座：模板 HTML 含 `{{avatar}}`（可配 `{{#if:avatar}}` 条件块）时自动把用户头像嵌入渲染；模板不需要头像则不显示；编辑页实时预览同样套用
- **所有职业默认模板已内置头像位**：产品/技术/销售/金融 4 套默认模板（含产品手动测试版）的头部均有 `{{avatar}}` 头像位，任意职位用户上传头像后都会在套用模板上展示；无头像时头像位自动隐藏

### 确认项 1：退出登录
- 操作：登录 PC → 页头右侧点「退出登录」
- 预期：短暂 loading 后跳回登录页；再次访问 /my-resume 等受保护页被路由守卫拦回登录页；服务端会话已删除（GET /api/session authenticated=false）
- 本地验证（2026-08-10 PASS，headless Chrome CDP）：页头有「退出登录」按钮 → 点击后跳 /login，会话未认证

### 确认项 2：按模板字段结构编辑简历 + 基础信息字段保存
- 操作：登录 PC → 我的简历 → 编辑简历
- 预期：
  - 顶部「基本信息」拆成 12 个独立字段输入框（姓名/性别/出生年月/电话/邮箱/所在地/在职公司/职位/工作年限/期望薪资/求职意向/到岗时间），按已保存字段回填（脱敏值显示为 138****8000、l***@example.com 等）
  - 下方各区块按字段编辑：工作经历/项目经历/教育经历等数组型区块可增删条目并上移/下移，每条例出公司/职位/时间/职责等字段；技能/证书等为多行文本框（每行一项）；区块可添加/删除/上移/下移
  - 切换模板后表单自动补齐该模板用到的区块字段（对号入座）；页面提示「按所套用模板的字段结构编辑」「基础信息已拆解为独立字段保存」
  - 底部实时预览：字段编辑后自动按当前套用模板渲染
- 保存：点「保存简历」→ 按表单字段组装结构化 + 文本保存，跳回我的简历；重新进入编辑页字段仍按保存值回填；服务端将 basic 独立字段落库（app_users.resume_basic）
- 本地验证（2026-08-10 PASS，CDP）：编辑页 12 个基础字段回填（姓名=李四、电话脱敏、邮箱脱敏、性别、年限、公司、职位、职责多行、项目、教育、技能多行）；改姓名预览联动更新；保存后 basic.name 仍为李四，DB resume_basic 含 12 字段

### 确认项 3：模板套用与切换（持久化）
- 操作：登录 PC → 我的简历（账号职位已有多套模板时）/ 编辑简历
- 预期：默认套用该职业「默认模板」（模板视图），模板切换器列出该职业全部模板（含来源徽标：默认/AI 生成/人工编辑），点其它模板立即切换渲染；**切换后写入用户 preferred_template_id**，刷新/再次进入仍加载上次套用的模板；我的简历与编辑简历共用该选择；无多余模板时（仅 1 套）不显示切换器
- 本地验证（2026-08-10 PASS，CDP 20/20）：product 职业 2 套模板（默认 AI 版 + 手动测试版），编辑页切换器 2 按钮、初始默认 AI 版；点手动版后 PUT /api/resume/template 持久化（preferredTemplateId=manual-product-dd981c0b）、预览标题与 iframe 重新渲染；到 /my-resume 默认加载手动版；切回 AI 版后 preferredTemplateId 复位
- 说明：模板切换持久化到用户个人（preferred_template_id），不改动后台 is_default

### 确认项 4：简历头像上传与模板套用
- 操作：登录 PC → 我的简历 → 「简历头像」卡片 → 点「上传头像」选一张图片（JPG/PNG/WebP/GIF，≤5MB）
- 预期：
  - 上传成功显示头像预览；刷新页面后头像仍在（GET /api/resume 返回 avatar.url）
  - 所套用模板 iframe 内头像位置自动出现该头像（产品/技术/销售/金融默认模板均已内置 {{avatar}} 头像位）；切换模板（含手动版）、再次进入仍套用；无头像时头像位自动隐藏
  - 点「更换头像」可换图；头像预览刷新后仍在；前端**不提供「删除头像」按钮**（按用户要求移除，仅保留上传/更换）
  - 选非图片文件或超过 5MB：明确提示「请选择图片格式（JPG/PNG/WebP/GIF）」「头像图片不能超过 5MB」，不产生脏数据
- 编辑简历页底部实时预览同样渲染头像（所套模板需要时）
- 本地验证（2026-08-10 PASS，API + headless Chrome CDP）：上传 PNG → GET /api/resume 返回 avatar.url → GET /api/resume/avatar 200（image/png + immutable 缓存）→ 模板 iframe 内 `<img class="avatar">` 渲染（naturalWidth>0）→ 编辑页预览渲染 → 非图片 400、超 5MB 400；DELETE /api/resume/avatar（后端删除接口保留，字段与磁盘文件均清理）→ 前端无「删除头像」按钮（CDP 实测仅「上传头像」/「更换头像」）
- 修复（2026-08-10）：①「更换头像/删除头像」按钮垂直错位 4px（全局 `label { margin: var(--space-4) 0 var(--space-2) }` 给 label 按钮加了上下边距，button 没有）→ `.avatar-upload-btn { margin: 0 }` 修复，实测两按钮 top 完全对齐；②头像未在模板展示（技术/销售/金融默认模板无头像位）→ 4 套默认模板 + product 手动版统一加 `{{avatar}}` 头像位，CDP 实测 tech/product（含手动版）模板 iframe 均渲染头像，渲染逻辑验证 5 套模板有/无头像两种情况全部正确
- 变更（2026-08-10 用户要求）：**移除「删除头像」按钮** —— 头像卡片仅保留上传/更换，删除走后端 DELETE /api/resume/avatar（API 仍可用）

### 本地验证结果汇总（2026-08-10）
- 接口：POST /api/logout（删会话+清 cookie）✓；GET /api/resume/templates（同职业全部模板 + preferredTemplateId）✓；PUT /api/resume/template（持久化个人套用模板）✓；GET /api/resume 返回 basic（resume_basic 优先）✓；PUT /api/resume 保存时将 structured.basic 拆解为 resume_basic 落库 ✓；POST/DELETE /api/resume/avatar（头像上传/更换/删除 + 图片类型与 5MB 校验）✓；GET /api/resume/avatar（头像图片服务 + immutable 缓存）✓
- 前端 CDP：编辑页 27/27（12 字段回填/预览联动/保存落库）+ 模板切换 20/20（编辑页+我的简历双端切换持久化）+ 头像（上传/更换/无删除按钮/模板 iframe 套用/编辑页预览/类型与大小校验）
- 产物：src/frontend/server/public/pc/（index-Dj9nsFym.js + index-CMkC8fJN.css）
- 人工核对入口：http://127.0.0.1:3215 登录（e2e-edit-product@local.dev / Test1234!）→ 我的简历 / 编辑简历；管理后台 http://127.0.0.1:3216（「简历模板」可新建/编辑模板供前端切换）

---

## 2026-08-10 · 上传简历识别职位并套用职业默认模板（PC 端）【本地验证 PASS，待用户确认】

**环境**：PC http://127.0.0.1:3215（本地）→ 生产 gwj.zhicha.io

### 开发要求（长期有效，禁止回退）
- 用户上传/更新简历后，系统识别简历所属职位（复用既有职业识别：resume_structured.occupation 优先，缺失时按脱敏文本识别）
- 若该职位在管理后台设置了「默认简历模板」，PC「我的简历」页自动用该模板渲染简历（默认视图），并保留「原文排版」可切换
- 未设置默认模板的职位：仍显示原文排版（回退视图），不出现切换按钮
- 模板渲染使用已脱敏的结构化数据；脱敏片段（如 138****5678）高亮可点击填写真实值，仅存本机浏览器 localStorage，不上传服务器
- 打印/保存 PDF 按当前视图分流：模板视图 → iframe 打印（模板自带 A4 @media print）；原文排版 → window.print()
- 本次仅 PC，H5 不开发

### 确认项 1：有默认模板的职位自动套用
- 操作：登录 PC → 我的简历（账号简历职位为 tech/product/sales/finance 等已设默认模板的职位）
- 预期：默认显示「默认模板」视图（切换按钮默认选中「默认模板」），页面顶部有「已套用职业模板」徽标与模板名称；下方 iframe 内按模板排版渲染简历（姓名/联系方式/技术矩阵/工作经历/项目/教育等），手机号邮箱已脱敏
- 本地验证（2026-08-10 PASS，headless Chrome CDP 14/14）：e2e-edit-tech@local.dev 登录 → /my-resume 默认模板视图，iframe 高度 1164px，含职业概述/工作经历/项目经验/教育背景；脱敏片段 3 处可点击填写；可切「原文排版」再切回

### 确认项 2：无默认模板的职位回退原文排版
- 操作：登录一个职位未设默认模板的账号（如 design）→ 我的简历
- 预期：不出现「默认模板/原文排版」切换按钮，直接显示原文排版
- 本地验证（2026-08-10 PASS，接口层）：临时 design 职位用户 → /api/resume/template 返回 applied:false、occupation=design、template=null

### 确认项 3：脱敏填写与打印
- 操作：模板视图中点击高亮脱敏内容（如 138****5678）→ 输入真实手机号 → 点「打印或保存 PDF」
- 预期：点击后提示输入真实值，填写后模板中显示真实值且底色变绿；刷新页面后仍保留（本机 localStorage）；打印走模板自带 A4 版式
- 说明：填写内容只存本机浏览器，按简历版本签名隔离，不写入服务器

### 本地验证结果汇总（2026-08-10）
- 接口：GET /api/resume/template 登录后返回 applied:true（tech/product 等已设默认）+ 模板 html；无模板职位 applied:false；未登录 401
- 前端渲染：CDP 实测 14/14 PASS（默认模板视图、iframe 内容完整、高度自适应、切换原文排版再切回、脱敏片段可填写）
- 修复：resumeBlocks.js 工作区残留的未定义 buildStyledBlocks 调用已回退（此前会导致「我的简历」页报错 buildStyledBlocks is not defined）；PC 渲染器新增 nl2br（数据里字面 \n 转 <br>，如 Node.js\nVue3）
- 产物：src/frontend/server/public/pc/（index-DHkCWUuq.js + index-ByiDpVAm.css）
- 人工核对入口：http://127.0.0.1:3215 登录（e2e-edit-tech@local.dev / Test1234!）→ 我的简历

## 2026-08-09 · 简历模板多套管理 + 职业默认模板（AI 生成多套，每职业可设默认）【本地验证 PASS，待用户确认】

**环境**：管理后台 http://127.0.0.1:3216（本地）→ 生产 /admin/

### 开发要求（长期有效，禁止回退）
- 每个职业可有多套 HTML 排版模板：内置 1 套（builtin）+ AI 生成多套（ai）+ 人工编辑（manual），互不覆盖
- 每个职业必须且只能有一套「默认」模板（唯一索引约束；删除默认套时自动补位为该职业最早一套）
- 后台「简历模板」页左侧按职业分组列出全部模板（默认套带「默认」徽标）；右侧支持：预览、编辑 HTML 并保存、AI 生成新排版（不覆盖现有）、设为默认、恢复内置、删除
- AI 生成的新模板为独立记录（source=ai），不覆盖现有任何模板；恢复内置仅覆盖内置记录，AI/人工版本保留
- 本次不接用户端真实简历渲染

### 确认项 1：职业默认模板
- 本地验证（PASS）：GET /resume-templates 返回 10 个职业分组，tech 有 1 套内置且 isDefault=true、defaultId=tech
- 操作：登录管理后台 → 左侧「简历模板」→ 查看各职业分组
- 预期：每个职业下列出该职业全部模板，默认套显示绿色「默认」徽标

### 确认项 2：AI 生成多套新排版
- 本地验证（部分，未配 AI）：POST /resume-templates/tech/generate 未配置 AI 时返回 503 明确错误「尚未配置 AI 接口…」，且不新增记录；待你在「AI 设置」配好文本模型/Key 后真验
- 操作：选中某职业任一套 → 点「AI 生成新排版」→ 可填风格要求 → 确认
- 预期：生成一套全新排版并出现在该职业分组列表中（来源=AI，不覆盖原有模板）；可继续预览/编辑/设为默认

### 确认项 3：设为默认
- 本地验证（PASS）：手工插入第二套后 set-default 切换成功，旧默认 isDefault=false，新默认 isDefault=true
- 操作：选中某套模板 → 点「设为默认」
- 预期：该套成为该职业默认（带「默认」徽标），原默认徽标消失；同职业始终只有一套默认

### 确认项 4：删除与自动补位
- 本地验证（PASS）：删除非默认套不影响默认；删除默认套后自动把该职业剩余最早一套设为默认
- 操作：对某套点「删除」→ 确认
- 预期：该套从列表移除；若删的是默认套，该职业自动补位一套新默认（默认徽标迁移）

### 确认项 5：恢复内置
- 本地验证（PASS）：POST /resume-templates/tech/reset 恢复为内置版本（source=builtin，html 还原，不抢默认标记）
- 操作：选中内置记录 → 点「恢复内置」
- 预期：内置内容还原；AI/人工版本不会被删除，仍留在列表中

### 本地验证结果汇总（2026-08-09，接口层 31 项冒烟全过，.codex/tmp/smoke_templates.mjs）
- 登录 → 列表 10 职业分组、tech 1 套内置默认 → 详情含 isDefault → 示例数据 → 不存在 id 404
- AI 生成：未配 AI 返回 503 且不新增；不存在职业 404
- 插入第二套 → 列表 2 套、默认不变；set-default 切换默认；删除非默认套；删除默认套自动补位
- reset 恢复内置；PUT 保存 → source=manual；空 html → 400
- 人工核对入口：http://127.0.0.1:3216 登录 → 左侧「简历模板」；AI 生成需先在「AI 设置」配置文本模型与 Key

## 2026-08-09 · 美化 HTML 简历模板（管理后台 预览 / 编辑 / AI 重新生成）【本地验证 PASS，待用户确认】

**环境**：管理后台 http://127.0.0.1:3216（本地）→ 生产 /admin/

### 开发要求（长期有效，禁止回退）
- 10 个职业模板各有一套美化版 HTML 简历模板；模板=完整 HTML（内联 CSS + {{field}} 占位符 + {{#list}}...{{/list}} 循环）
- 管理后台「简历模板」页可：预览（示例数据渲染）、编辑 HTML 并保存、AI 重新生成（复用后台 AI Key/模型配置）、恢复内置模板
- 内置模板与 AI/手动修改互不覆盖：启动时仅 upsert 内置模板，不覆盖 ai/manual 来源记录
- 本次不接用户端真实简历渲染（后续另行接入，模板占位符与结构化字段对齐）

### 确认项 1：模板列表与预览
- 本地验证（PASS）：冒烟接口 GET /resume-templates 返回 1 套 tech（source=builtin，HTML 11514B）；页面左侧列表 + 右侧 iframe 预览（示例数据林晓芸渲染）已实现。
- 操作：登录管理后台 → 左侧「简历模板」→ 切换 10 个职业
- 预期：每个职业显示对应美化模板预览（示例数据渲染，非空白/乱码）

### 确认项 2：编辑并保存
- 本地验证（PASS）：PUT /resume-templates/tech 保存后 source 变 manual；恢复内置后 source 回 builtin、内容还原（无手动标记残留）。
- 操作：点「编辑」，修改模板 HTML（如改标题颜色），点「保存」
- 预期：提示保存成功，预览立即反映修改；刷新页面后修改仍在（来源变为手动）

### 确认项 3：AI 重新生成
- 本地验证（部分，未配 AI）：POST /resume-templates/tech/regen 未配置 AI 时返回 503 明确错误「尚未配置 AI 接口。请先在『AI 设置』中添加并启用 API Key 与默认模型。」；待你在「AI 设置」配好文本模型/Key 后真验。
- 操作：点「AI 重新生成」（后台已配置可用的文本模型/Key），可输入附加风格要求
- 预期：调用 AI 后返回新的模板排版并预览刷新；未配置 AI 时给出明确错误提示

### 确认项 4：恢复内置
- 本地验证（PASS）：POST /resume-templates/tech/reset 恢复为内置版本（source=builtin，html 还原，无手动标记）。
- 操作：对 AI/手动修改过的模板点「恢复内置」
- 预期：模板恢复为系统内置版本，预览更新

### 本地验证结果汇总（2026-08-09，接口层 9 项冒烟全过，.codex/tmp/test-smoke.mjs）
- 登录成功 → 列表 1 套（tech）→ 示例数据（林晓芸）→ 详情（source=builtin）
- 保存 → source=manual；恢复内置 → source=builtin 且内容还原（种子不覆盖 ai/manual）
- AI 重新生成：未配置 AI 时返回 503 明确错误（需配好 Key/模型后真验）
- 不存在的模板 id → 404
- 人工核对入口：http://127.0.0.1:3216 登录 → 左侧「简历模板」→ tech 模板预览/编辑/恢复内置；AI 重生成需先在「AI 设置」配置文本模型与 Key

## 2026-08-09 · 页头岗位分析每次进入必须空白（硬性需求，防回退）

**环境**：https://gwj.zhicha.io（PC 端）

### 开发要求（长期有效，禁止回退）
- 报告邮件通知可重新发送（2026-08-09 新增）：报告详情/列表提供「重新发送邮件」入口；频率限制：**两次发送至少间隔 10 分钟，同一报告一天最多发送 5 次**；达到限制时给出明确提示，禁止绕过

- 报告「重新分析」行为约定（2026-08-09 再次确认）：我的报告列表点「重新分析」→ 基于原报告的岗位内容 + 用户最新简历，生成一条**新的**报告记录；原报告保留不覆盖，列表多出一条；仅新报告（已保存岗位内容）显示按钮，上线前旧报告无岗位内容故无按钮（数据限制，非功能缺失）

- 页头「岗位分析」每次点击都必须进入**空白**岗位表单，不得读取/带出上一次分析的岗位信息。
- 首页「录入岗位」、「我的报告 → 分析新岗位」、邮箱验证后「继续使用」等新分析入口，同样必须空白。
- 报告页「修改材料后重分析」仍要保留本次报告的岗位内容（可编辑后重新分析），该回填不允许被误删。

### 确认项 1：页头岗位分析为空白
- 操作：有历史岗位记录时，点页头「岗位分析」
- 预期：进入「确认目标岗位」页，职位描述 / 公司简称 / 岗位名称全部为空

### 确认项 2：岗位页内再点页头岗位分析
- 操作：已在岗位页且表单有内容时，再点页头「岗位分析」
- 预期：表单立即清空为空白

### 确认项 3：首页「录入岗位」
- 操作：首页主按钮显示「录入岗位」时点击进入
- 预期：岗位表单为空白

### 确认项 4：报告页「分析新岗位」
- 操作：我的报告页点「分析新岗位」→ 确认事实 → 进入岗位
- 预期：岗位表单为空白

### 确认项 5：修改材料后重分析（防误删）
- 操作：报告页点「修改材料后重分析」
- 预期：岗位内容仍保留本次报告的岗位内容（可编辑），不是空白

## 2026-08-09 · 报告邮件通知重新发送（已上线）

**环境**：https://gwj.zhicha.io（PC 端）

### 确认项 1：列表出现「重新发送邮件」按钮
- 操作：登录后进入「我的报告」
- 预期：每份有接收邮箱的报告行右侧显示「重新发送邮件」按钮；邮件状态旁显示「（今日 x/5）」

### 确认项 2：重新发送成功
- 操作：点击某报告的「重新发送邮件」（距上次发送超过 10 分钟、今日未满 5 次）
- 预期：按钮短暂显示「发送中…」，随后弹窗「邮件已重新发送，请注意查收」；邮件状态变「已发送」，今日计数 +1

### 确认项 3：10 分钟间隔限制
- 操作：收到邮件后立即再次点击「重新发送邮件」
- 预期：弹窗提示「邮件发送过于频繁，请至少间隔 10 分钟，约 X 分钟后再试」，不会发出新邮件

### 确认项 4：一天 5 次上限
- 操作：同一报告当天累计发送（含首次自动发送）满 5 次后再点
- 预期：按钮禁用并提示「今日发送已达上限」；邮件标签显示「（今日 5/5）」；绕过前端直接调接口也会被服务端 429 拒绝

### 确认项 5：移动端
- 操作：手机浏览器打开「我的报告」
- 预期：H5 未开发（待你确认后再动工）；PC 移动宽度下按钮整行、布局不溢出

### 补充说明
- 频率限制在服务端强制（禁止绕过）：两次发送至少间隔 10 分钟、同一报告一天最多 5 次（含报告生成时的首次自动发送）
- 只有发送成功才计入次数；发送失败（如邮箱服务异常）会显示失败原因，不计入限制，可立即重试

### 线上验证结果（2026-08-09 已发布上线）
- 已部署：commit e047bb9；PM2 gangweijing 重启（node --env-file=.env），DB 幂等新增 email_sent_times jsonb 列
- 接口验证 11/11 PASS：列表字段 canResendEmail / emailSentToday / emailMaxToday；日上限 429 EMAIL_DAILY_LIMIT；间隔不足 429 EMAIL_INTERVAL_LIMIT；未登录 401；不存在 404；发送失败分支 502（不计入次数）
- 页面验证 5/5 PASS：有邮箱报告行显示「重新发送邮件」按钮；无邮箱报告行无按钮；邮件状态标签存在；新前端资源 index-C2COxrzM.js 已加载

## 2026-08-09 · 报告重新分析 + 分析中禁用返回（已上线）

**环境**：https://gwj.zhicha.io（PC 端）· 公测阶段分析不限次

### 确认项 1：旧报告不显示「重新分析」
- 操作：登录后进入「我的报告」页面
- 预期：功能上线前生成的历史报告，每行**没有**「重新分析」按钮（旧报告未保存岗位内容）

### 确认项 2：新报告显示「重新分析」
- 操作：从「分析新岗位」走一次完整流程（确认事实 → 提交岗位 → 生成报告），或对已有可重新分析的报告操作后回到列表
- 预期：新生成的报告行右侧显示「重新分析」按钮

### 确认项 3：点击重新分析
- 操作：在报告列表点击某条报告的「重新分析」
- 预期：①按钮变灰并显示「分析中…」，不可重复点击；②分析完成后自动跳转到**新生成的报告页**；③回到列表多出一条同岗位的新报告（时间更新）

### 确认项 4：分析中「返回事实」不可点
- 操作：提交岗位进入 AI 分析中，点击「返回事实」按钮
- 预期：按钮处于禁用状态，无法在分析中途返回（本次与重新分析一起上线）

### 确认项 5：移动端
- 操作：手机浏览器打开同一页面
- 预期：H5 未开发（待你确认后再动工），本次不涉及；PC 移动端宽度下按钮占整行、布局不溢出

### 补充说明
- 重新分析基于「原报告的岗位内容 + 你**最新已保存**的简历」生成，属于一份独立的新报告，原报告保留
- 仅生成新报告时有真实 AI 调用与成本（公测期间不设限）


## 2026-08-09 · 报告删除改为「标记无效/软删除」（已上线）

**环境**：https://gwj.zhicha.io（PC 端）· 管理后台 /admin

### 开发要求（长期有效，禁止回退）
- 后台删除报告**不得物理删除数据库记录**，改为给报告标记无效（deleted_at）。
- 用户端「我的报告」前端也要提供删除入口，效果等同：删除后该报告在前端不再展示。
- 前端一律过滤已删除报告：我的报告列表不展示；分享/详情链接无效；不能重新分析、不能重新发送邮件。
- 数据保留在数据库中（可 SQL 恢复），删除不是销毁。

### 确认项（上线后人工验收）
1. 后台删除：管理后台报告列表删除某报告 → 后台列表不再展示该报告，用户端「我的报告」也不再展示
2. 前端删除：用户端「我的报告」报告行点「删除」→ 确认后该行消失
3. 已删报告不可访问：分享链接/详情/重新分析/重新发送邮件均不可用

### 线上验证结果（2026-08-09 已发布上线）
- 已部署：主服务 release 20260809021051（server.js/db.js/public-pc 新产物 index-CbHnzDPj.js + index-DoYpl6ct.css）；admin 覆盖 db.js + dist 新产物（index-CloOjCes.js）；PM2 两个服务均 delete+start
- 生产 DB：app_reports.deleted_at 列已由幂等 ALTER 自动创建（总计 11 份真实报告，deleted=0）
- 接口验证 16/16 PASS（临时 e2e 账号+测试报告，测后已清理）：
  1. 未登录删除 401；用户删除 200 ok；DB 行保留且 deleted_at 写入
  2. 用户列表不再展示；公开详情 404；重新分析 404；重新发送邮件 404；重复删除 404
  3. 后台列表过滤已删报告；后台详情 404；后台删除 200 且 deleted_at 写入；后台删除后用户端列表也消失
  4. 恢复 deleted_at=NULL 后报告重新出现（可恢复性验证）
- 线上前端资源确认：PC 端 /pc/ 引用 index-CbHnzDPj.js，admin /admin/ 引用 index-CloOjCes.js，均 200

## 2026-08-09 · 报告邮件重发 10 分钟冷却（已上线）· 重新分析一次性机会（已上线）

**环境**：https://gwj.zhicha.io（PC 端）

### 开发要求（长期有效，禁止回退）
- 已发送过邮件通知的报告，距上次发送不足 10 分钟时，「重新发送邮件」按钮置灰浅灰色、不可点击；10 分钟到期后恢复可点。
- 「重新分析」每份报告仅一次机会（含重新分析生成的新报告）：一旦使用过（reanalyzed_at 已记录），该报告及新生成报告的「重新分析」按钮直接隐藏，服务端 429 强制拦截（REANALYZE_ALREADY_USED），禁止绕过。

### 确认项 1：邮件重发 10 分钟冷却按钮置灰
- 操作：对刚收到过邮件（10 分钟内）的报告查看「我的报告」列表
- 预期：该行「重新发送邮件」按钮为浅灰色、不可点击，悬停提示「距上次发送不足 10 分钟，约 X 分钟后可再次发送」；10 分钟过后恢复可点

### 确认项 2：重新分析仅一次机会，用后按钮隐藏
- 操作：点击某报告「重新分析」进入任务 → 任务完成后回到「我的报告」列表
- 预期：原报告（及新生成报告）的「重新分析」按钮消失（隐藏）；绕过前端直接调接口返回 429「该报告已使用过重新分析机会。」（REANALYZE_ALREADY_USED）

### 补充说明
- 邮件重发冷却剩余秒数由服务端下发（列表字段 emailResendWaitSeconds），前端按秒数置灰，服务端强制兜底禁止绕过
- 重新分析成功会同时记录源报告与新生成报告的 reanalyzed_at，两者均视为已使用机会，防止链式连续重分析刷成本
- 首次生成报告（/api/analyze）不受次数限制；「修改材料后重分析」走新分析流程，不受此限

### 线上验证结果（2026-08-09 已发布上线，release 20260809031303）
- 已部署：主服务 release 20260809031303（server.js/db.js + public/pc 新产物 index-B1_lyZ6t.js + index-DWWwF-3s.css），PM2 gangweijing delete+start，服务 online
- 部署核验：GET /、/pc/、新 JS 均 200；server.js 已删除 REANALYZE_MIN_INTERVAL_MS、reanalyze 接口已改为 REANALYZE_ALREADY_USED 一次性拦截；DB 列 reanalyzed_at 存在；列表/详情已不再下发 reanalyzeWaitSeconds
- 说明：历史冷却版 release 20260809023004 已上线；本版将「重新分析 10 分钟冷却」升级为「每份报告仅一次机会，用后按钮隐藏」，邮件重发 10 分钟冷却保留

## 2026-08-09 · 后台公告前端展示 + 记录用户已知悉（已上线）

**环境**：https://gwj.zhicha.io（PC 端）

### 开发要求（长期有效，禁止回退）
- 管理后台「系统设置 → 公告内容」填写后，PC 前端登录用户需看到公告。
- 用户点击「我知道了」后记录该用户已读（app_users.announcement_ack_at）；公告内容更新后，用户需重新确认一次。
- 公告为空时前端不弹窗、不干扰用户。H5 不开发，不涉及。

### 确认项 1：公告展示
- 操作：管理后台填写公告内容并保存 → PC 端登录用户进入首页
- 预期：弹出公告弹窗，完整展示后台填写的公告内容，仅提供「我知道了」按钮（无关闭跳过）

### 确认项 2：已知悉记录
- 操作：点击「我知道了」→ 关闭弹窗后刷新页面 / 重新登录
- 预期：不再弹公告；数据库 app_users.announcement_ack_at 已写入

### 确认项 3：公告更新后重新确认
- 操作：后台修改公告内容并保存 → 用户重新登录（或刷新）
- 预期：公告弹窗再次出现，需再次点击「我知道了」

### 确认项 4：公告为空不弹
- 操作：后台公告内容清空保存 → 用户登录
- 预期：无公告弹窗，正常使用

### 线上验证结果（2026-08-09 已发布上线，release 20260809031303）
- 已部署：主服务 release 20260809031303（含 GET /api/config 与 POST /api/announcement/ack）+ admin db.js 更新（announcement_updated_at），PM2 gangweijing / gangweijing-admin delete+start，均 online
- 部署核验：GET /api/config 下发公告内容与 announcementUpdatedAt/announcementAckAt；DB 列 announcement_ack_at（app_users）+ announcement_updated_at（admin_settings）均已创建
- 说明：生产已有公告内容，历史公告（announcement_updated_at 为空）未确认过的用户登录会弹一次，确认后不再弹；后续公告更新会重新要求确认


## 2026-08-09 · 去除邮件配置环境变量兜底（已上线，release 20260809032024）

**环境**：https://gwj.zhicha.io（PC 端 + 管理后台）

### 开发要求（长期有效，禁止回退）
- 邮件发送（验证邮件 / 报告完成通知 / 重新通知）仅使用后台数据库配置（admin_settings.resend_api_key / email_from），不再读取环境变量 RESEND_API_KEY / EMAIL_FROM 兜底
- 管理后台「系统设置 → 服务配置状态」仅展示数据库配置，不再展示「环境变量兜底」状态
- 邮件配置只能通过后台「邮件配置（Resend）」维护；.env 中的 RESEND_API_KEY / EMAIL_FROM 不再生效

### 背景说明（本次问题根因）
- 用户此前在后台看到的「已配置」实际来自环境变量兜底：生产 .env 配置了 RESEND_API_KEY / EMAIL_FROM，数据库 admin_settings.resend_api_key 一直为空（之前从未在后台保存过）
- 本次已把 .env 中生效的邮件配置迁移写入数据库：resend_api_key（36 位）+ email_from=report@zhicha.io，去除兜底后邮件功能不中断

### 线上验证结果（2026-08-09 已发布上线，release 20260809032024）
- 已部署：主服务 release 20260809032024（server.js 去除兜底）+ admin（server.js 移除 environment 下发 + dist 新产物 index-CeLuf8Rc.js），PM2 delete+start 均 online
- 数据迁移：admin_settings 已写入 resend_api_key（SET len=36）+ email_from=report@zhicha.io，verify 通过
- 部署核验：主服务 server.js 无 process.env.RESEND_API_KEY / EMAIL_FROM 引用；admin server.js 与 dist 均无 emailEnvConfigured；GET /、/pc/、/admin 均 200

## 2026-08-09 · 修复后台邮箱配置「丢失」隐患：禁止启动/部署重置 admin_settings（已修复，待部署）

**环境**：https://gwj.zhicha.io（管理后台 + PC 端）

### 问题背景（用户反馈）
- 用户反馈「服务配置状态」里之前配置过的邮箱信息丢失，怀疑发布部署时执行脚本把数据重置了。
- 排查结论：
  1. 部署脚本（deploy_*.sh）只做文件复制 + PM2 重启，从不执行任何数据库重置 SQL；git 历史无 DROP/TRUNCATE/DELETE admin_settings。
  2. 生产库此前 admin_settings.resend_api_key 一直为空（从未在后台保存成功过），邮件实际靠 .env 环境变量兜底；后台「环境变量兜底」行此前显示「已配置」，故用户以为配置在数据库。
  3. 唯一「启动时可能改写 admin_settings 用户数据」的代码是 admin db.js 的自愈 UPDATE：它会在站点名损坏时空/问号时连带清空 announcement 并刷新 updated_at——属危险模式，本次已收窄。

### 开发要求（长期有效，禁止回退）
- admin_settings 为后台关键用户配置表（站点名 / 公告 / 邮件密钥 / 并发数），任何启动 schema 或部署脚本都禁止重置其中的 announcement / resend_api_key / email_from 等用户字段。
- 站点名自愈仅允许修复 site_name 本身，不得连带清空其他列或刷新 updated_at。
- 邮件配置只能通过后台「邮件配置（Resend）」维护；.env 中的 RESEND_API_KEY / EMAIL_FROM 不再生效（上一条目）。

### 确认项 1：服务配置状态显示已配置
- 操作：登录管理后台 → 系统设置 → 服务配置状态
- 预期：邮件密钥（数据库）显示「已配置」，发件人 report@zhicha.io 正常展示

### 确认项 2：重启后配置不丢失
- 操作：部署/重启后台服务后再次查看
- 预期：邮件密钥与公告内容保持不变，updated_at 不被无谓刷新

### 确认项 3：公告内容保持
- 操作：后台公告内容查看
- 预期：已填写的公告内容不被清空

### 线上验证结果（2026-08-09 已发布上线，commit e67dac5）
- 已部署：admin db.js 收窄版（自愈仅修 site_name），PM2 gangweijing-admin delete+start，服务 online；主服务无改动未重启
- 部署核验：线上 /var/www/gangweijing-admin/db.js 自愈 SQL 已收窄（SET site_name 仅修复站点名）；生产库 admin_settings 完好：site_name=岗位镜、公告 86 字符保留、resend_api_key=SET(len36)、email_from=report@zhicha.io、analysis_concurrency=2、updated_at 未被无谓刷新；admin / 200
- 说明：部署脚本本身从不执行数据库重置 SQL（已核实 deploy_*.sh 仅文件复制+PM2 重启，git 历史无 DROP/TRUNCATE/DELETE admin_settings）；生产库邮件配置此前为空系从未在后台保存过、靠 .env 兜底，上一任务已迁移写入，本次彻底消除启动逻辑误伤用户配置的隐患
## 2026-08-09 · 简历按行业模板编辑（开发完成已提交，待确认发布上线）

**环境**：https://gwj.zhicha.io（PC 端）

### 开发要求（长期有效，禁止回退）
- 「我的简历」页新增「编辑简历」入口，进入按系统识别的行业模板分区编辑（仅 PC，H5 不开发）
- 编辑表单按匹配到的行业模板（技术/产品/销售/金融/设计/职能/医疗/应届/管理/通用）的区块顺序与字段组织；模板未覆盖的原文内容以「未覆盖内容」文本块保留，保存后内容只多不少
- 保存后简历文本按模板区块顺序重组；服务端照常脱敏、职业识别，事实确认状态按既有规则重置
- 编辑保存走现有 PUT /api/resume（前端提交结构化，服务端跳过 AI 重新结构化，保留用户精确编辑、不重复计费）

### 确认项 1：入口
- 操作：登录 PC → 我的简历
- 预期：工具栏出现「编辑简历」按钮（有简历时可用）

### 确认项 2：按行业模板分区编辑
- 操作：点击「编辑简历」
- 预期：页面顶部显示匹配到的行业模板名称；表单按该模板区块顺序展示基本信息/摘要/技能/工作经历/项目/教育等，字段可编辑；模板未覆盖的原文内容以「未覆盖内容」文本块保留

### 确认项 3：编辑并保存
- 操作：修改某字段（如工作经历职责、技能），点「保存简历」
- 预期：保存成功回到我的简历，**立即按新的编辑内容重新排版浏览**（文本按模板区块顺序重组），无内容丢失
- 本地验证（2026-08-09 PASS，后端 3215 构建产物 + CDP 实测）：编辑保存后自动跳回我的简历页即渲染新内容（新增摘要/技能均即时生效），排版按行业模板顺序（tech：摘要→技能→工作经历→项目经历…），原内容完整保留

### 确认项 4：数据与脱敏
- 操作：保存后查看我的简历 / 重新分析
- 预期：修改内容用于后续分析；手机号/邮箱等仍脱敏展示；已确认的事实状态按既有规则重置


## 2026-08-09 · AI 模板生成真验 + AI 设置接口调用配置（1M 长文本 / 超时放大）【本地验证 PASS，待用户确认】

**环境**：管理后台 http://127.0.0.1:3216（本地）→ 生产 /admin/

### 开发要求（长期有效，禁止回退）
- 新增简历模板：选职业 → AI 生成 / 直接粘贴编辑 HTML → 预览；AI 生成必须走后台「AI 设置」中已配置的默认模型 + Key
- AI 设置「接口调用」配置：支持 1M 长文本（请求体限 5MB）；接口调用超时秒数可配置（30-3600，默认 300），按实际耗时放大等待
- 每个职业默认使用自己的模板（默认徽标），不共用一套

### 确认项 1：AI 生成走后台配置的模型/Key
- 本地验证（PASS）：POST /resume-templates/product/generate 返回 200，耗时 121.3 秒，生成 HTML 21961 字符（记录 ai-product-84b516e5 保留在库）
- 操作：管理后台 →「简历模板」→ 选中「产品 / 运营」职业 → 点「AI 生成新排版」→ 填风格要求 → 确认
- 预期：生成一套全新排版并出现在该职业分组列表（来源=AI，不覆盖），可预览 / 编辑 / 设为默认

### 确认项 2：AI 设置「接口调用」超时配置
- 本地验证（PASS）：GET /api/admin/settings 返回 ai_call_timeout_seconds=300；PUT 300 成功；PUT 5 / 9999 返回 400「接口调用超时需为 30-3600 秒。」
- 操作：管理后台 →「AI 设置」→ 找到「接口调用」卡片 → 修改超时秒数 → 保存
- 预期：显示「已保存」；刷新后仍保留该值；生成模板时按该超时等待（实测约 121 秒，默认 300 秒足够）

### 确认项 3：长文本 1M 支持
- 本地验证（PASS）：模板新增/编辑/生成三个接口 body limit=5MB，单模板 HTML 上限 1MB
- 操作：在「新增模板」或「编辑 HTML」中粘贴较大 HTML（>100KB）
- 预期：正常保存，不出现 413 请求体过大错误

### 本地验证结果汇总（2026-08-09）
- 接口：settings GET/PUT 超时字段、PUT 边界校验 30-3600 全部通过
- AI 生成真验：200、121.3s、21961 字符、记录保留
- 前端：vite build 通过，AI 设置页「接口调用」卡片渲染正常（CDP 验证 #call-timeout=300）
- 人工核对入口：http://127.0.0.1:3216 登录 → 「AI 设置」→「简历模板」

---

## 2026-08-11 · 管理后台「生成任务」列表页（后台任务队列查看管理）【本地验证 PASS，待用户确认】

**环境**：管理后台 http://127.0.0.1:3216（本地）→ 生产 /admin/

> 背景：用户澄清——「前端页面不需要排队任务管理页面」指用户端（PC/H5 前台）不做该页面；**管理后台需要任务列表页面**。

### 开发要求（长期有效，禁止回退）
- 管理后台新增「生成任务」页面（侧边栏导航，路由 /admin/task-queue）：查看 AI 生成简历模板任务的执行情况
- 统计卡片：全部 / 排队中 / 生成中 / 已完成 / 失败 / 已取消 六类数量，点击卡片可筛选
- 状态筛选 chips + 自动刷新开关（有排队/生成中任务时每 4 秒自动刷新）+ 手动刷新按钮
- 任务表格：任务（职业名 + ID）、状态徽标、创建时间、耗时、结果（生成的模板名 / 错误信息）、操作
- 操作：排队中任务可「取消排队」；失败/已取消任务可「重试」；「清空历史」清除已完成/失败/已取消（保留排队中与生成中）
- 内存保留最近 50 条任务（服务重启后清空）
- 用户端（PC/H5）不新增任何任务队列管理页面

### 确认项 1：入口与页面
- 操作：管理后台 → 左侧导航「生成任务」
- 预期：进入 AI 生成任务页，含统计卡片（全部/排队中/生成中/已完成/失败/已取消）、状态筛选、自动刷新开关、刷新、清空历史；无任务时显示空状态引导
- 本地验证（2026-08-11 PASS，headless Chrome CDP）：登录进入 /admin/task-queue，6 张统计卡片、空状态、刷新/清空历史按钮、自动刷新开关均渲染正常

### 确认项 2：任务展示与刷新
- 操作：在「简历模板」页提交一个 AI 生成任务后，到「生成任务」页点「刷新」
- 预期：任务行出现（职业名、状态徽标、创建时间、耗时、结果），统计卡片计数同步更新
- 本地验证（2026-08-11 PASS）：提交 sales 生成任务（返回 jobId、status=running）→ 页面点刷新 → 表格出现 1 行「销售 / 市场 · 红橙业绩风 · 生成中」，全部=1、生成中=1

### 确认项 3：取消 / 重试 / 清空历史
- 操作：排队中的任务点「取消排队」；失败或已取消的任务点「重试」；点「清空历史」
- 预期：取消后状态变「已取消」；重试后重新入队（带重试标记）后台执行；清空历史仅清除已完成/失败/已取消
- 本地验证（2026-08-11 PASS，API）：连续提交 2 个任务（1 running + 1 pending）→ 取消 pending 任务成功（状态 canceled）→ 重试成功（pending + retriedFrom 标记）→ 状态筛选 pending 计数正确 → 清空历史 ok（活动任务保留）
- 接口：POST /api/admin/resume-templates/generate-jobs/:id/cancel、/:id/retry、/clear-history；GET /generate-jobs[?status=] 原有

### 人工核对入口
- 本地：http://127.0.0.1:3216 登录（admin@jobmirror.local）→ 「生成任务」
- 生产：https://gwj.zhicha.io/admin → 「生成任务」


---

## 2026-08-11 · 统一任务列表（菜单「任务列表」，前后端长任务全部纳管）【本地验证 PASS，待用户确认】

**环境**：管理后台 http://127.0.0.1:3216（本地）→ 生产 /admin/

> 背景：用户要求菜单名称叫「任务列表」；前后端所有有长任务处理的环节都纳入这个查看管理列表，做好状态同步即可。

### 开发要求（长期有效，禁止回退）
- 菜单名称统一为「任务列表」（侧边栏 /admin/task-queue，页面标题「任务列表」），不再叫「生成任务」/「AI 生成任务」
- 任务统一落库 `app_jobs` 表（PG 持久化，服务重启不丢），状态机：`pending → running → done | error | canceled`
- 涉及前后端所有长任务/异步/耗时处理都写入该表并同步状态，当前已纳管 5 类：
  1. AI 模板生成（admin）`template_generate`：FIFO 队列串行执行，进度/结果/错误全程同步
  2. 报告生成（用户端 analysisQueue+worker）`report_generate`：入队 pending → worker 执行 running → done/error，job id = reportId
  3. 简历上传解析 `resume_parse`：解析完成/失败同步状态（result 含 fileRef/mode）
  4. 岗位截图识别 `screenshot_ocr`：识别完成/失败同步状态（result 含 companyShortName）
  5. 简历文本结构化 `resume_structure`：结构化完成/失败同步状态
- 旧接口兼容保留：`/resume-templates/generate-jobs*` 4 个接口仍返回旧字段格式（模板列表页轮询未改动），改从 PG 读取
- 启动恢复：admin 只标 `template_generate` 中断任务；frontend 只标 `report_generate` 中断任务（不误标其他服务任务）
- 用户端（PC/H5）不新增任何任务队列管理页面

### 确认项 1：入口与菜单名
- 操作：管理后台 → 左侧导航
- 预期：菜单显示「任务列表」（不是「生成任务」）；进入 /admin/task-queue 页面标题为「任务列表」，含类型筛选（全部类型/AI 模板生成/报告生成/简历解析/截图识别/简历结构化 6 项）、状态筛选（全部/排队中/执行中/已完成/失败/已取消）、搜索框、6 张统计卡片、自动刷新（4s，有活动任务时）、手动刷新、分页（每页 50）
- 本地验证（2026-08-11 PASS，headless Chrome CDP）：侧边栏「任务列表」、页面标题、6 统计卡片、类型筛选 6 chips、状态筛选 5 chips、搜索框、自动刷新、分页器均渲染正常，类型筛选点击生效

### 确认项 2：AI 模板生成任务纳入统一列表
- 操作：「简历模板」页提交 AI 生成任务 → 「任务列表」查看
- 预期：任务出现在统一列表（任务类型=AI 模板生成），状态 pending → running → done 全程可见，结果含生成的模板名；旧 generate-jobs 接口同步可见同一批任务
- 本地验证（2026-08-11 PASS）：提交 sales 生成 → 统一列表 pending → running → done + result(templateId/templateName) 约 104 秒

### 确认项 3：用户端长任务状态同步
- 操作：PC 上传简历解析 / 提交岗位分析 / 重新分析 / 结构化保存后，到后台「任务列表」查看
- 预期：分别出现「简历解析」「报告生成」「简历结构化」任务行，状态随执行同步为 done/error；报告生成任务 job id = reportId，入队即 pending
- 本地验证（2026-08-11 PASS）：上传 PDF 解析 → resume_parse done；/api/analyze + reanalyze → report_generate pending ×2；jobs/stats 汇总正确

### 确认项 4：分页 / 筛选 / 统计 / 清空
- 操作：任务列表页使用类型筛选、状态筛选、搜索、翻页、清空历史
- 预期：筛选与分页生效，统计卡片计数正确；清空历史仅清除 done/error/canceled，保留 pending/running
- 本地验证（2026-08-11 PASS）：分页/类型/状态筛选、stats 汇总、clear-history 全部通过（API 冒烟）

### 人工核对入口
- 本地：http://127.0.0.1:3216 登录（admin@jobmirror.local）→ 「任务列表」
- 生产：https://gwj.zhicha.io/admin → 「任务列表」


---

## 2026-08-11 · 测试用户标记 + 用户邀请机制 + 仅测试账号可删除【已上线，待用户确认】

**环境**：管理后台 https://gwj.zhicha.io/admin → 「用户管理」

### 开发要求（长期有效，禁止回退）
- 测试用户标记（app_users.is_test）+ 邀请外链（?invite=）绑定邀请人；后台用户管理展示「邀请人 / 邀请人数」（列表与详情均有，含邀请码/链接复制）
- 后台不提供「标记测试 / 取消测试」操作；**只有测试账号显示删除按钮**，删除接口对非测试用户返回 403「仅测试用户可删除。」

### 确认项
- 操作：后台 → 用户管理
- 预期：列表含「类型（测试/正式）、邀请人、邀请人数」列；非测试用户无删除按钮；测试用户有删除按钮；用户详情页显示邀请码/邀请链接/邀请人/邀请人数
- 生产验证（2026-08-11 PASS）：GET /users 返回 invite_count/invited_by_email；PATCH /users/:id 已下线（404）；DELETE 非测试用户 403 且未删除；DELETE 测试用户 200

### 人工核对入口
- 生产：https://gwj.zhicha.io/admin → 「用户管理」
