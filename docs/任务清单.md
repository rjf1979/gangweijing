1、我的分析报告页面中，状态筛选和分析新岗位按钮布局需要优化

2、点击分析新岗位后，如果还没上传简历跳转到上传简历流程页面，反之跳转到上传新岗位流程页面

3、首页注册增加免责声明：主要是每个用户有免费三次岗位分析，全程都是AI分析，只能作为参考，大模型存在成本开销后续会有收费可能性，介绍所用大模型：gpt-5.6或者deepseek v4，会根据实际情况系统自动切换，所有内容本站有最终解释权，多增加以下免责声明，并且告知本站只是一个工具，非权威官方或者组织。




## 简历 HTML 排版美化（2026-08-08 已完成并验证）

约束：不改动 resume_text 脱敏原文、不做文本内容优化，仅排版优化；数据源 = 脱敏文本 + 已有 resume_structured；不新增 AI 调用。
机制：结构化优先 + 文本兜底；容错解析 + 自适应区块注册表/自由区块 + 内容保护 + 自我完善（观测埋点）。

4、✅ PC「我的简历」改为 HTML 结构化简历排版（A4 商务版式）：
   - 有 structured 用结构化渲染；无/失败回退现有纯文本模式（原功能保留）
   - 区块：头部联系信息 / 个人摘要 / 工作经历(时间线) / 项目经历 / 教育经历 / 技能 / 证书 / 获奖 / 自我评价
   - 空区块自动隐藏

5、✅ 容错解析：字段类型/空值归一（null、非数组、'无'/'暂无'/'-' 等噪音）；标题别名归一（工作经历/工作经验/职业经历…）

6、✅ 自适应区块注册表 + 自由区块：通用区块注册表（标题别名 + 文本识别规则 + 渲染器）；扫描脱敏文本识别注册表之外的新标题（培训经历/社团活动/志愿者/专利…）自动生成自由区块

7、✅ 内容保护：未被任何区块吸收的文本段落自动归入「附加信息」区块，内容只多不少

8、✅ 脱敏标注与复原填写保留：行内分段（正常文字 + 脱敏类型标签 + 可填写 input）；填写仅存本机 localStorage（版本签名隔离）不上传；打印/保存 PDF 输出排版后 HTML 简历（填好复原值的纯值版），规避浏览器不打印 input 值

9、✅ 自我完善（一档观测埋点）：渲染后记录区块覆盖率（空区块、未吸收文本、unknownTitles 新标题），供后续沉淀进注册表（三档）

10、构建与发布：PC 构建 + 产物同步 public/pc；H5 不动；提交 + 部署 + 生产抽测（本地三场景 CDP 验证通过：结构化/文本兜底/空简历）