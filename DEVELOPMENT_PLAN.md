# 岗位镜开发计划

## 当前目标

交付一个可注册、可上传材料、可通过 GPT 生成可核实分析报告，并可邮件通知用户的轻量 Web 工具。

## 实施顺序

1. 账户与材料：邮箱注册、私有上传、简历文本提取、岗位截图 OCR。
2. 事实确认：从简历提取职业事实卡片，用户确认后才进入岗位比对。
3. 证据对照：逐条显示简历证据、岗位要求、AI 判断、待核实项。
4. 优化交付：提供不虚构事实的简历段落改写建议，并支持用户采纳或忽略。
5. 报告通知：保存报告摘要，通过 Resend 邮件通知用户查看。
6. 数据参考：仅在授权数据完整、可追溯时显示市场参考，不参与个人录用预测。

## 当前进度

- 已完成邮箱注册、登录与 30 天会话。
- 已完成 PDF/DOCX 简历提取和 GPT 岗位截图识别。
- 已完成 GPT 适配分析、专属报告链接和邮件链接推送。
- 已完成个人报告列表、分析状态与邮件状态展示。

## 非目标

- 自动投递、批量爬取职位或候选人数据。
- 把岗位匹配度表述为 Offer 成功率。
- 基于未授权职位抓取数据进行对外推荐、训练或营销。
## UI 验证后优先开发任务

> 来源：.runtime/shots 截图布局审查（2026-08-07）。优先级：P0 阻塞验证/核心使用，P1 移动端与覆盖，P2 审计质量。

### P0-1 重新建立真实视口截图验证

- [ ] 修改 .runtime/visual-shot.mjs，增加 captureBeyondViewport: false 的视口截图模式。
- [ ] 保留全页截图，但文件名区分为 *-full.png，避免把页面总高度误认为设备视口。
- [ ] 固定视口：1440x900、1024x768、375x812、横屏 844x390。
- [ ] 重新生成截图，确认 PNG 尺寸、摘要 css.width/css.height 与目标视口一致；横屏必须为 844x390。
- [ ] 验收：每个核心页面都有真实视口和全页截图；运行 node .runtime/visual-shot.mjs 并检查 .runtime/shots/summary.json。

### P0-2 登录页标题两行文字重叠修复

- [ ] 修复登录页 `#welcome-title` 两行标题“先登录，再开始分析。”的行高与字形重叠问题；重点检查 `styles.css` 中 `.auth-intro h1` 的 `line-height: 1.02` 以及 375px 断点下的字号组合。
- [ ] 将两行标题的实际行框、字形可视区域和 mark 背景分别检查，确保第二行不会压住第一行，也不会被荧光背景裁切。
- [ ] 在 375px、768px、1024px、1440px 和 844x390 横屏真实视口截图中验证；同时检查登录错误提示出现时标题不发生遮挡。
- [ ] 验收：两行文字之间保留清晰的垂直间距，文字无相互覆盖、无背景覆盖字形、无换行抖动；减少动效模式下结果一致。

### P0-3 完成核心页面真实视口布局回归

- [ ] 回归 /、/facts、/job、/report、/reports、/my-resume 首屏，确认标题、主 CTA、表单/报告主体不被头部或加载层遮挡。
- [ ] 检查 /facts、/job、/report 长文本的换行、卡片高度、按钮位置和底部内容。
- [ ] 375px 下确认页面级 scrollWidth === innerWidth，不允许整体横向滚动；844x390 横屏确认上下滚动和操作按钮可用。
- [ ] 验收：核心内容首屏可识别，主 CTA 可操作，无出界元素、文字裁切和遮挡。

### P1-1 优化移动端步骤条可发现性

- [ ] 保留窄屏步骤条横向滚动，但增加右侧渐隐、边缘箭头或当前步骤计数提示。
- [ ] 当前步骤进入页面后必须可见；状态继续使用文字，不能只依赖颜色。
- [ ] 检查步骤条与页面主滚动的手势冲突，触摸目标与步骤间距保持至少 8px。
- [ ] 验收：375px 下用户能理解步骤条并可靠操作；键盘 Tab 顺序与视觉顺序一致。

### P1-2 补齐异常、空状态和验证页截图覆盖

- [ ] 生成 /reports 无报告空状态桌面/移动端截图：reports-empty-1440、reports-empty-375。
- [ ] 生成邮箱验证成功、失败/过期状态桌面/移动端截图，至少覆盖 verify-email/bad-token。
- [ ] 复核 state-loading、state-login-error、state-job-error、state-focus-*、state-reduced-motion：错误靠近问题区域，恢复操作可见，焦点环不裁切，减少动效不跳动。
- [ ] 验收：异常状态有原因和恢复动作，空状态有下一步入口，摘要记录标题和状态。

### P1-3 修正视觉审计脚本的可见标题判定

- [ ] 修改 .runtime/visual-shot.mjs 的 visibleHeading，只读取 display、visibility、尺寸及祖先隐藏状态均有效的 h1/h2。
- [ ] 验收：facts、job、report、reports、my-resume 摘要标题对应当前页面，不再误读隐藏登录标题；登录页仍显示登录标题。

### P2-1 建立可重复的布局审计结果

- [ ] 将横向溢出、文本裁切、小触控目标、焦点环、加载层和 reduced-motion 结果写入结构化 JSON。
- [ ] 摘要分别记录 viewportWidth/viewportHeight、contentWidth/contentHeight、isFullPage。
- [ ] 缺失截图场景必须使审计失败，不得静默跳过。
- [ ] 验收：一次运行输出通过/失败项目、失败元素选择器、修复建议，并区分 P0/P1。
